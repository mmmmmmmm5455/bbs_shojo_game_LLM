# 测试清单 v2（可更新）

> **用法**：把 `[ ]` 改成 `[x]` 表示通过；失败在同一行用 `<!-- 备注 -->` 或简短中文说明。  
> **启动**：在仓库根目录执行 `cd src`，再 `python main_advanced_v2.py`。  
> **版本说明（相对 v1）**：BGM 在子系统初始化**之后**加载；CRT 整体减轻且随碎片增加而减弱；立绘为**连续插值**变清晰；打字挑战中**不**弹时间事件、**不**随机触发 VH；随机 VH 概率已降低。

**本轮测试日期**：____年____月____日

---

## A. 启动与环境

| # | 步骤 | 结果 |
|---|------|------|
| A1 | 首次启动无报错、窗口标题正常 | [ ] |
| A2 | 底部聊天区、记忆条、右上「记忆分辨率」框可见 | [ ] |
| A3 | 按 `H` 打开帮助，再按 `H` 或 `ESC` 关闭 | [ ] |

---

## B. 记忆碎片 ↔ 立绘（C1 相关）

| # | 步骤 | 结果 |
|---|------|------|
| B1 | 开局记录右上小立绘「粗糙」程度 | [ ] |
| B2 | 连续发送 5～10 条普通聊天（非命令），每条约 +1 碎片 | [ ] |
| B3 | 对比 B1：小立绘应**平滑**变清晰（非突然跳档才算通过） | [ ] |
| B4 | 若有存档：`S` 保存 → 重启 → `L` 加载，立绘与碎片条一致 | [ ] |

---

## C. CRT 与画面后处理（C2 相关）

| # | 步骤 | 结果 |
|---|------|------|
| C1 | 碎片接近 0 时：整屏有轻微复古感，但**不应**糊到难读 | [ ] |
| C2 | 碎片提高后（多发消息或完成打字）：CRT 应**可感知地**减轻 | [ ] |
| C3 | 按 `P` 暂停：画面稳定、无崩溃 | [ ] |

---

## D. ASCII 与情绪（G3 / G5）

| # | 步骤 | 结果 |
|---|------|------|
| D1 | 静观 15 秒，中央绿色 ASCII 有换帧感 | [ ] |
| D2 | 发送含「谢谢、开心」等词 → 表情偏积极 | [ ] |
| D3 | 发送含「难过、抱歉」等词 → 表情偏消极 | [ ] |
| D4 | 按 `M` 静音开关若干次，无崩溃 | [ ] |

---

## E. MIDI 与 BGM（G4 相关）

| # | 步骤 | 结果 |
|---|------|------|
| E1 | 按 `G` 或发 `/bgm`：**应能**打开背景 MIDI（非「未就绪」） | [ ] |
| E2 | 再按 `G` 或 `/bgm` 关闭 BGM | [ ] |
| E3 | 发：`/midi note C4 q note E4 q note G4 q`，能听到短旋律（设备有声时） | [ ] |
| E4 | 故意 `/midi xyz`，出现 `[MIDI]` 错误提示且不崩溃 | [ ] |

---

## F. 垂直保持 VH（G6 + 不打断）

| # | 步骤 | 结果 |
|---|------|------|
| F1 | 按 `V`：数秒内画面有滚屏/失调感 | [ ] |
| F2 | 按 `R`：恢复稳定 | [ ] |
| F3 | 按 `K` 进入打字挑战，**挂机 2 分钟**：不应出现随机 VH、不应弹出「特殊事件」全屏 | [ ] |
| F4 | 退出打字（`ESC`）后，再挂机 3～5 分钟：随机 VH 应**很少**出现（非完全没有） | [ ] |

---

## G. 打字挑战与记忆奖励

| # | 步骤 | 结果 |
|---|------|------|
| G1 | `K` 开始挑战，**不打回车**，打完最后一个字 → 应弹出结算界面 | [ ] |
| G2 | 结算后记忆条有增加 | [ ] |
| G3 | 挑战进行中按普通字母：不误触论坛等热键（输入模式正确） | [ ] |

---

## H. 论坛、存档、终端入口

| # | 步骤 | 结果 |
|---|------|------|
| H1 | `F` 打开论坛，`ESC` 返回 | [ ] |
| H2 | `S` 保存，`L` 加载，主要状态恢复 | [ ] |
| H3 | `B` 启动终端版（源码运行时应成功；打包 exe 可能提示不可用，属预期） | [ ] |

---

## I. 可选：打包回归

| # | 步骤 | 结果 |
|---|------|------|
| I1 | `src` 下执行 `python -m PyInstaller --noconfirm main_advanced_v2.spec` 成功 | [ ] |
| I2 | 运行 `dist/BBSShojoGame/BBSShojoGame.exe` 能进主界面 | [ ] |

---

## 汇总表（快速勾选）

| 区块 | 本块全过请打勾 |
|------|----------------|
| A 启动 | [ ] |
| B 立绘 | [ ] |
| C CRT | [ ] |
| D ASCII/情绪 | [ ] |
| E MIDI/BGM | [ ] |
| F VH/不打断 | [ ] |
| G 打字 | [ ] |
| H 论坛存档 | [ ] |
| I 打包（可选） | [ ] |

---

**清单维护**：改勾选或日期即可；若需求变更可再升版为 v3。
